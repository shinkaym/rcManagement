## Rules:
- Don't auto edit files, this project for study
- Only provide guidance and blueprints; do not modify code unless I explicitly request code changes.
- For UI code, prefer `StyleSheet` over inline styles directly on components.

## Migration Notice:
- This React Native project is currently being migrated away from Expo.
- Before proposing any refactor direction, carefully inspect the existing files, dependencies, and packages in use to make sure the recommendation matches the project's current migration state.
